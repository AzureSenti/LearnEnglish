<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_word_set"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/space_sm"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/sanctuary_surface"
    app:cardCornerRadius="@dimen/sanctuary_card_corner"
    app:cardElevation="3dp"
    app:rippleColor="@color/sanctuary_primary_blue_light"
    app:strokeColor="@color/sanctuary_card_stroke"
    app:strokeWidth="0.5dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/space_md">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <FrameLayout
                android:id="@+id/icon_container"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/bg_icon_container_blue">

                <ImageView
                    android:id="@+id/iv_folder"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_folder"
                    app:tint="@color/sanctuary_primary_blue" />
            </FrameLayout>

            <ImageView
                android:id="@+id/iv_more"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_alignParentEnd="true"
                android:contentDescription="@string/action_edit"
                android:padding="2dp"
                android:src="@drawable/ic_more_vert"
                app:tint="@color/sanctuary_text_secondary" />
        </RelativeLayout>

        <TextView
            android:id="@+id/tv_set_name"
            style="@style/TextAppearance.Sanctuary.Lexend.H3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_md"
            android:textSize="15sp"
            tools:text="IELTS Prep" />

        <TextView
            android:id="@+id/tv_word_count"
            style="@style/TextAppearance.Sanctuary.Inter.Caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_xs"
            tools:text="420 words" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
