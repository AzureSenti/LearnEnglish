<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnEnglish"
        android:usesCleartextTraffic="true"
        >

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.mainmenu.MainMenuActivity"
            android:exported="false" />

        <activity
            android:name=".feature.auth.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".feature.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".feature.auth.ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".feature.onboarding.OnboardingActivity"
            android:exported="false" />

        <activity android:name=".feature.wordsets.LibraryActivity"
            android:exported="false" />

        <activity
            android:name=".feature.articles.ArticlesActivity"
            android:exported="false" />

        <activity
            android:name=".feature.articles.ArticleDetailActivity"
            android:exported="false" />

        <activity
            android:name=".feature.wordsets.WordSetDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.ProfileActivity"
            android:exported="false" />

        <!-- Grammar -->
        <activity android:name=".feature.grammar.GrammarRoadmapActivity"
            android:exported="false" />

        <activity android:name=".feature.grammar.GrammarTheoryActivity"
            android:exported="false" />

        <activity android:name=".feature.grammar.GrammarQuizActivity"
            android:exported="false" />


    </application>

</manifest>
